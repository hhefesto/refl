---
id: order
title: "Order World"
dependencies: [equality]
options:
  agda: ["--safe", "--without-K"]
  lean: []
---
`_≤_` as an inductive relation, its properties, decidability, and the Boolean reflection idiom (`_≤ᵇ_`, `T`, `≤ᵇ⇒≤`) that `Decoding.agda` is built on. Ends with `Decoding.keep-max`, the Boolean-reflection cliff.

*Planned world: the levels below have their learning goals written; statements, hints and solutions are the next authoring pass.*
