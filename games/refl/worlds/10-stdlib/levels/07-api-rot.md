---
id: api-rot
index: 7
title: "Bug hunt: stdlib 1.5 → 2.3"
learning_goals:
  - "`Function.Equivalence` gone, `Inverse` field renames, `Algebra.Lattice` split, `Reflects {a = ℓ}`, `IsSemiringWithoutAnnihilatingZero` flattening"
---
*Planned level.* Learning goals:

- `Function.Equivalence` gone, `Inverse` field renames, `Algebra.Lattice` split, `Reflects {a = ℓ}`, `IsSemiringWithoutAnnihilatingZero` flattening

<!-- @conclusion -->
