`a = b` is an equality proposition, a type asking for a proof that `a` and
`b` are equal. The `:` in `theorem name : a = b := by …` introduces the
proposition. The block after `by` constructs its proof.
