`{a == b : Nat}` is an equality type: it claims that the two natural-number
endpoints are equal. The inner colon specifies their type. This is distinct
from `{==}`, which is a proof of reflexivity. A `law` states the type and
a `def` of the same name supplies its proof.
