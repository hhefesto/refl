`ℕ` is the type of natural numbers. `zero : ℕ` is its first constructor;
`suc : ℕ → ℕ` takes a natural number and returns its successor (one more).
Thus `2` abbreviates `suc (suc zero)`. Parentheses group an argument.
