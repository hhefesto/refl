```agda
≡-irrelevant : ∀ {x y : ℕ} (p q : x ≡ y) → p ≡ q
```
Hedberg's theorem for `ℕ`, proved without axiom K from decidable equality. Standard library: `Axiom.UniquenessOfIdentityProofs.Decidable⇒UIP.≡-irrelevant _≟_`.
