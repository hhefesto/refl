`a ≡ b` is an equality type, a proposition claiming that `a` and `b` are
equal. A proof is a value of that type. In `proof : a ≡ b`, the colon gives
the type of `proof`; it does not supply the proof. `_≡_` names the infix
operator: the underscores mark the positions of its two arguments.
