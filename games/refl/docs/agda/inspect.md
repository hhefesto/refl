```agda
f args with e | inspect g x
... | true | [ eq ] = …
```
The pre-2.6 way to remember a `with` result; `Reveal_·_is_` / `[_]` live in `Relation.Binary.PropositionalEquality`. Read as `with … in eq`.
